# Personal documents — never commit
*.pdf
uploads/
extracted/
*.zip

# Real CV data — the app writes these. They contain personal details and must
# never land in this public repo. Seed data belongs in SEED_CV, nowhere else.
cv-data.json
*_All-Applications_*.json
images/

# Secrets
.env*
!.env.example

# Local editor and tool directories
.vscode/
.idea/
.cache/
.*-local/

# OS junk
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# Node (if tooling is ever added)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.gstack/
